name: Deploy peds_edu_app to EC2

on:
  push:
    branches:
      - production_new     # change to main/testing if that's your deploy branch

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Debug EC2_SSH_KEY presence
        run: |
          if [ -z "${{ secrets.EC2_SSH_KEY_PROD }}" ]; then
            echo "ERROR: EC2_SSH_KEY is EMPTY or not visible to this workflow."
            exit 1
          else
            echo "EC2_SSH_KEY is set. Length: $(echo "${{ secrets.EC2_SSH_KEY_PROD }}" | wc -c) characters"
          fi

      - name: Write SSH key to file
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.EC2_SSH_KEY_PROD }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H ${{ secrets.EC2_HOST_PROD }} >> ~/.ssh/known_hosts || true

      - name: Sync code to EC2
        run: |
          rsync -az --delete \
            -e "ssh -i ~/.ssh/id_rsa -o StrictHostKeyChecking=no" \
            --exclude '.git' \
            --exclude '.github' \
            --exclude '.idea' \
            --exclude 'venv' \
            --exclude 'media' \
            --exclude 'staticfiles' \
            ./ ${{ secrets.EC2_USER_PROD }}@${{ secrets.EC2_HOST_PROD }}:${{ secrets.EC2_TARGET_DIR_PROD }}

      - name: Run deploy script on EC2
        run: |
          ssh -i ~/.ssh/id_rsa -o StrictHostKeyChecking=no \
            ${{ secrets.EC2_USER_PROD }}@${{ secrets.EC2_HOST_PROD }} \
            "cd ${{ secrets.EC2_TARGET_DIR_PROD }} && chmod +x deploy.sh && ./deploy.sh"
